# train_delay_data_project
Ingests live (or near-real-time) UK train data, processes it into useful features, trains a model to predict delays, serves the predictions through a simple dashboard or API
